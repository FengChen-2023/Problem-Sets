import java.util.*;

class Solution
{
  	public static void main( String args[] )
  	{
    		Scanner sc = new Scanner( System.in );

        System.out.println( "The name of the person is " + sc.next() + " and the age is " + sc.nextInt() + "." );
	  }
}
